#include<stdio.h>
#include<math.h>
int main()
{
    int a;
    scanf("%d",&a);
    float s,v;
    s=(6*a*a);
    v=pow(a,3);
    printf("Surface area = %.2f and Volume = %.2f",s,v);
   // printf("Volume = %.2f",v);
}